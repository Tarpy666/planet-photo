This repo implements the planet synthesis module,
providing planetary data flow primitives for the harness.
Contains a runnable Python main() that demonstrates planet pipeline operations.
Uses stdlib only; no pip dependencies.
